Requirements for installing and using the service:
    1) In your local PostgreSQL database, create a new db called "travel_log_db".
    2) In order for the downloaded project to run on your local computer without errors,
        run the "gradle build" command in the Gradle command line.
    3) To test all endpoints of the project,
        you can use the file created for the example with the name "example-reguesc.http" in the "resources" folder.
